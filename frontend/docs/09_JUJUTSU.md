# Git Worktrees

Active frontend workflows use Git/GitHub only for version-control, isolation, and PR delivery.

Use a Git branch for normal work and a Git worktree only when physical directory isolation is required.
